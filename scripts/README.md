# scripts/

Scripts Python / shell du projet.

- `ingest.sh` — ingestion des Parquet NYC Taxi vers `RAW` (à venir, brique J1).
- `explore_data.py` — exploration des données / EDA (à venir, brique J1).

> Contrat d'interface : l'ingestion s'appelle via `bash scripts/ingest.sh <mois…>`.
