# Python / uv
__pycache__/
*.py[oc]
.venv/
venv/
build/
dist/
*.egg-info/

# Secrets — NE JAMAIS committer (clés RSA, mots de passe, SAS…)
secrets/
*.p8
*.pem
.env
.env.local
# Profils dbt locaux : chacun crée le sien depuis profiles.yml.example.
# (git ne gère pas les commentaires en fin de ligne → ils doivent être sur leur propre ligne.)
# Exception : le profil factice de la CI (dbt_nyc_taxi/ci/profiles.yml) est VERSIONNÉ.
profiles.yml
!dbt_nyc_taxi/ci/profiles.yml

# Données volumineuses (téléchargées à la demande)
data/raw/
*.parquet

# dbt
dbt_nyc_taxi/target/
dbt_nyc_taxi/dbt_packages/
dbt_nyc_taxi/logs/
logs/
# identifiant anonyme local dbt (télémétrie coupée, mais filet de sécurité)
.user.yml

# OS / éditeur
.DS_Store
.vscode/
.idea/